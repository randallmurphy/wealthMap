import React from 'react';
import { Box, Typography } from '@mui/material';

const Home = () => {
  return (
    <Box p={4}>
      <Typography variant="h4">Home</Typography>
      <Typography>Track assets, liabilities, income, and expenses.</Typography>
    </Box>
  );
};

export default Home;
